services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: test-benchmark:local
    container_name: test-benchmark-web
    environment:
      PORT: "8000"
    ports:
      - "${TEST_BENCHMARK_WEB_HOST_PORT:-18110}:8000"
    restart: unless-stopped
